{
  "name": "cvan-blog",
  "version": "1.0.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git://github.com/cvan/blog.git"
  },
  "scripts": {
    "start": "wintersmith preview",
    "build": "wintersmith build",
    "predeploy": "git checkout master && git pull && sed -e 's/\"url\":.*$/\"url\": \"https:\\/\\/blog.cvan.io\",/g' config.json > config_prod.json && wintersmith build --config config_prod.json && rm -f config_prod.json && git checkout gh-pages && cp -R build/ . && rm -rfv build/ CNAME && echo 'blog.cvan.io' > CNAME && git add . && git commit -m 'update site' && git push origin head && open https://blog.cvan.io && git checkout -",
    "deploy": "npm run build"
  },
  "devDependencies": {
    "moment": "^2.3.1",
    "typogr": "^0.5.2",
    "underscore": "^1.4.4",
    "wintersmith": "^2.3.3"
  }
}
